> In-Memory [[Key Storage|Key-Value Database]]

- All values are stored entirely in [[Memory]]
	- there _are_ ways to persist data through [[Snapshot|snapshots]] and "transaction logs"
- Usually used for [[Cache|Caching]] expensive/frequent queries to other databases

## Types
- By default, every value is a string
- [[Schlüssel|Key]] ist immer ein [[String]]