```json
[
  {
    "adapter": "Java",
    "request": "attach",
    "label": "Attach to Quarkus",
    "hostName": "localhost",
    "port": 5005,
    "cwd": "$ZED_WORKTREE_ROOT/horsey-go-brrrr-be"
  }
]
```

- run with:
	- "spawn task" (`alt-shift-t`)
	- choose "Attach to Quarkus"
	- should work

> [!warning] Requires the `jdtls` language server to be installed.

